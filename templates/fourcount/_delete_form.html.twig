<form method="post" action="{{ path('fourcount_delete', {'id': fourcount.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ fourcount.id) }}">
    <button class="btn btn-danger">Supprimer le fourcount</button>
</form>
