{% extends 'base.html.twig' %}

{% block title %}New Fourcount{% endblock %}

{% block body %}
    <h1>Create new Fourcount</h1>

    {{ include('fourcount/_form.html.twig') }}

    <a href="{{ path('user_index', {'id': fourcount.id}) }}" style="margin:5px;"><button>Créer un nouvel utilisateur</button></a>
    <a href="{{ path('fourcount_index') }}"><button>Retour à la liste</button></a>
{% endblock %}
