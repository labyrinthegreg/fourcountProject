{% extends 'base.html.twig' %}

{% block title %}Balance Chart{% endblock %}

{% block body %}
    <h2>Balance</h2>
    
    {% for key,value in balance %}
        <p>{{key}} | {{value}}</p>
    {% endfor %}
    <a href="{{ path('balance_download', {'id': fourcount.id}) }}" class="btn btn-primary">Exporter la balance</a>

{% endblock %}
